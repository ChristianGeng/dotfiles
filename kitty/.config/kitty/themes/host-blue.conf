# Dark blue background tint -- the demo / livekit host.
# Only the background is changed; foreground/syntax stay at your kitty defaults.
# Applied via: kitten @ set-colors -a host-blue.conf ; reverted with --reset.
background #0f1a2a
