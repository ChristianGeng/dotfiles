# Dark red background tint -- flags an expensive / shared host (e.g. p4d cluster).
# Only the background is changed; foreground/syntax stay at your kitty defaults.
# Applied via: kitten @ set-colors -a host-red.conf ; reverted with --reset.
background #2a0f0f
